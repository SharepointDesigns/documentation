name: Deploy Documentation

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"
          working-directory: spd-documentations

      - name: Install dependencies
        run: npm install
        working-directory: spd-documentations

      - name: Build
        run: npm run build
        working-directory: spd-documentations

      - name: Deploy
        run: npm run deploy
        working-directory: spd-documentations
